import 'package:flutter/material.dart';

// Main colors in the pallette

// Black shadows
const Color black = Color(0xFF121212);
const Color background = Color(0xFF1A1A1A);
const Color lightBackground = Color(0xFF202020);

// White shadows
const Color pureWhite = Color(0xFFFFFFFF);
const Color lightGray = Color(0xFFECECEC);
const Color gray = Color(0xFFE1E1E1);

// Accent colors
const Color neonGreen = Color(0xFFCEFF45);


/// *****************************************************

final Color backgroundColor = background;
final Color logoColor = neonGreen; // for now

// Input field colors
final Color inputTextFillColor = black;
final Color inputTextBorderColor = lightBackground;
final Color cursorColor = neonGreen;

// Buttons
final Color primaryButtonFillColor = black;
final Color secondaryButtonFillColor = lightBackground;
final Color tertiaryButtonFillColor = neonGreen;

// Card colors
final Color cardFillColor = lightBackground;
final Color avartIconFillColor = gray;
final Color avartTextColor = neonGreen;
final Color settingsIconColor = black;

// Avatar colors
final Color avatarFillColor = black;
final Color avatarTextColor = neonGreen;
final Color avatarBorderColor = lightBackground;

// Text colors
const Color textLight = pureWhite;
final Color textMedium = gray;
const Color textDark = lightBackground;
final Color hintTextColor = gray;
final Color primaryButtonTextColor = lightGray;
final Color secondaryButtonTextColor = black;

// App bar
final Color appBarColor = black;
final Color appBarTextColor = neonGreen;

// App bar icon color
final Color appBarIconColor = lightBackground;

// Dialog colors
final Color dialogBackgroundColor = lightBackground;
final Color dialogTextColor = lightGray;
